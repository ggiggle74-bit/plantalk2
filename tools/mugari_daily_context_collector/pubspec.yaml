name: mugari_daily_context_collector
description: Pure Dart collector and contract validator for Plantalk daily keyword contexts.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.5

dev_dependencies:
  lints: ^6.1.0
  test: ^1.31.2
